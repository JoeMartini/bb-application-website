# bb-application-website

## Installation

`npm install`

## Development

### Launch
`npm start`

## Build

`npm run build`

The production will built in the `build` directory. Deploy this three files to your static server:

- `index.html`
- `app.js`
- `main.css`
